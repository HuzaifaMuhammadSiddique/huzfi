  def factorial(n):
    """ This function calculates the factorial of a number. Factorial of a number is the product of one up till that number.
        Factorial cannot be negative. """

    factorial = 1

    if n >= 0:
      for x in range(1, n + 1):
        factorial = factorial * x
      return factorial

    else:
      print("Factorial of negative numbers is not possible!")
  
  
